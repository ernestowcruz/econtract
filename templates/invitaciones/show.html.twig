{% extends 'base.html.twig' %}

{% block title %}Invitaciones{% endblock %}

{% block body %}
    <h1>Invitaciones</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invitacione.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ invitacione.nombre }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ invitacione.correo }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ invitacione.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('invitaciones_index') }}">back to list</a>

    <a href="{{ path('app_register_user_empresa', {'link': invitacione.link}) }}">edit</a>

    {{ include('invitaciones/_delete_form.html.twig') }}
{% endblock %}
